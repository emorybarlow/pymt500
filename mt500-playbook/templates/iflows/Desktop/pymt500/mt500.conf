[mt500]
fips = {{ fips }}
network_id = {{ network_id }}
heartbeat_interval = {{ heartbeat_interval }}

[serial]
in = {{ serial_in }}
out = {{ serial_out }}

[consumers]
{% for consumer in consumers %}
{{ consumer }}
{% endfor %}
